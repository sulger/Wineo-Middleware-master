package fr.doranco.wineo.middleware.services;

import javax.xml.bind.JAXBContext;

import fr.doranco.wineo.middleware.objetmetier.bouteille.Bouteille;

public class Test {
	
	@org.junit.Test
	public void creerFichierXmlBouteille() {
		
		/*
		Bouteille bouteille = new Bouteille();
		bouteille.setAnnee(2012);
		bouteille.setContenance(0.75);
		bouteille.setDesignation("toto");
		bouteille.setReference("bipbop");
		
		JAXBContext context = JAXBContext.newInstance(Bouteille.class);
		
		// Je crée un fichier XML
		context.createMarshaller()
			.marshal(bouteille, monFluxVersMonFichier);
		
		// J'instancie un objet depuis un fichier XML
		Bouteille bouteilleLue = context.createUnmarshaller()
				.unmarshal(monFluxVersMonFichier);
		*/
		
	}

}
